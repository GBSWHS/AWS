name: approve
on:
  repository_dispatch:
    types: [approve-command]
jobs:
  approve:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Test
        run: |
          echo ${{ github.event.client_payload.github.payload.issue.number }}
      - name: Add comment
        uses: peter-evans/create-or-update-comment@v2
        with:
          issue-number: ${{ github.event.client_payload.github.payload.issue.number }} 
          body: |
            Action started.
      - name: Install dependency
        run: |
          cd actions/approve
          npm i
      - name: Create Instance
        run: |
          cd actions/approve
          node . '${{ toJSON(github.event.client_payload.github.payload.issue.body) }}' ${{ github.event.client_payload.slash_command.args.named.name }} 
